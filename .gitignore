# Data tree — large and regenerable via the pipeline (clips, corpora, embedding
# caches, models, external reference datasets). Only code + docs are tracked.
data/

# Live-proof artifacts (kept out of git; summarized in docs/PROOFS.md)
proofs/*.wav
proofs/*.npz
proofs/_work/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/

# Node / Playwright
node_modules/
test-results/
playwright-report/

# OS / editor
.DS_Store
*.swp

# generated artifacts
.coverage
docs/scorecard.json
